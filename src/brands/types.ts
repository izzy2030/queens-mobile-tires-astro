export type BrandConfig = {
    id: string;
    name: string;
    domain?: string;
    logoPath: string;
    primaryColor: string;
    accentColor: string;
    phone: string;
    email: string;
    address: string;
    cityLabel: string;
    googleMapsUrl?: string;
    gaId?: string;
    metaPixelId?: string;
};
